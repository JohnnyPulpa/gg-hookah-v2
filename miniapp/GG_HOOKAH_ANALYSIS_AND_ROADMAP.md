# GG HOOKAH v2 — Полный анализ проекта + Новый план работы

**Дата:** 20 февраля 2026  
**Подготовлено для:** Станислав (JohnnyPulpa)  
**Контекст:** Переход с браузерного Claude на Max план (Claude.ai + Claude Code)

---

## 1. ТЕКУЩИЙ СТАТУС ПРОЕКТА — ЧЕСТНЫЙ ОБЗОР

### Что реально сделано и работает ✅

| Компонент | Статус | Зрелость |
|-----------|--------|----------|
| Инфраструктура (сервер, SSL, Nginx, systemd) | ✅ Полностью | 95% |
| База данных (14 таблиц, миграции, seed data) | ✅ Полностью | 90% |
| Backend API (заказы, миксы, напитки) | ✅ Базовый | 60% |
| Mini App UI (React + TS + Tailwind, v15 дизайн) | ✅ Базовый | 65% |
| Telegram Bot (start, language, admin auth) | ✅ Каркас | 30% |
| Admin Panel (orders, sessions, menu CRUD) | ✅ Частично | 55% |
| Уведомления (bot → клиент при смене статуса) | ❌ Не начато | 0% |
| Support система (threaded messages) | ❌ Не начато | 0% |
| Hot actions (cancel, rebowl, pickup через бота) | ❌ Не начато | 0% |
| Guests management | ❌ Не начато | 0% |
| Discounts & Promo Codes (admin CRUD) | ❌ Не начато | 0% |
| Settings editor (admin) | ❌ Не начато | 0% |
| Logs/System page | ❌ Не начато | 0% |
| Background jobs (SESSION_ACTIVE → SESSION_ENDING) | ❌ Не начато | 0% |
| Тесты | ❌ Отсутствуют | 0% |

### Общий прогресс: ~45% от MVP по спеке

---

## 2. КРИТИЧЕСКИЙ АНАЛИЗ — ЧТО НУЖНО ИСПРАВИТЬ

### 🔴 Критические проблемы

**2.1. Отсутствует фоновая задача SESSION_ACTIVE → SESSION_ENDING**  
По спеке (3.7): каждые 1-5 минут нужно проверять активные сессии и переводить в SESSION_ENDING за 30 минут до конца. Без этого вся логика сессий сломана — клиент никогда не увидит опции rebowl/free extension/pickup.

**2.2. Бот не отправляет уведомления при смене статуса**  
Вся event-driven логика из спеки Section 3 не реализована. Админ меняет статус в панели, но клиент не получает сообщений. Это делает сервис неюзабельным.

**2.3. Нет связки Admin → Bot (webhook/event bus)**  
Когда админ меняет статус заказа, бот должен отправить уведомление клиенту. Сейчас admin (Flask на 5002) и bot (aiogram polling) — изолированные процессы без коммуникации.

**2.4. Standalone HTML шаблоны в Admin Panel**  
Каждый шаблон содержит полный HTML с sidebar/topbar inline (копипаста ~100 строк в каждом файле). Это осознанное решение "для простоты", но при 11+ шаблонах любое изменение sidebar требует редактирования всех файлов. B22 — яркий пример: нужно исправить ссылку Menu в 5 файлах.

### 🟡 Архитектурные улучшения

**2.5. Нет Jinja2 template inheritance в admin**  
Решение: переход на `base.html` + `{% extends %}`. Это одноразовая инвестиция, которая окупится при каждом следующем изменении.

**2.6. Backend API не валидирует Telegram initData**  
Mini App принимает заказы без проверки подлинности запроса от Telegram. Любой может отправить POST с fake telegram_id.

**2.7. Нет order_items при создании заказа**  
По спеке (7.3.6), заказ должен создавать записи в order_items (hookah + drinks). Нужно проверить, реализовано ли это.

**2.8. users таблица не используется**  
Спека определяет таблицу `users` для Telegram identity + language, но бот и Mini App не создают/обновляют записи. Языковые предпочтения хранятся только в localStorage клиента.

---

## 3. НОВАЯ СТРАТЕГИЯ РАБОТЫ: Claude.ai + Claude Code

### 3.1. Разделение ролей

```
┌─────────────────────────────────────────────────┐
│            Claude.ai (Браузер — ТЫ)             │
│                                                 │
│  • Планирование и архитектурные решения          │
│  • Ревью кода и отладка сложных багов           │
│  • Написание спеков для конкретных задач         │
│  • Генерация файлов (docx/md/pptx)              │
│  • Создание CURRENT_STATE обновлений            │
│  • Обсуждение дизайн-решений                    │
│                                                 │
│  Когда использовать:                            │
│  → "Что делать дальше?"                         │
│  → "Помоги разобраться с архитектурой X"         │
│  → "Напиши мне задание для Claude Code"          │
│  → "Обнови документацию"                        │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│         Claude Code (Терминал на сервере)        │
│                                                 │
│  • Написание и редактирование кода               │
│  • Запуск команд на сервере                      │
│  • Создание/миграция файлов                      │
│  • Тестирование (curl, запуск скриптов)          │
│  • Git операции (commit, push)                   │
│  • Диагностика сервисов                         │
│  • Рефакторинг (массовые изменения)             │
│                                                 │
│  Когда использовать:                            │
│  → Любая работа с файлами на сервере            │
│  → "Создай файл X с содержимым Y"               │
│  → "Запусти тест / проверь сервис"              │
│  → "Сделай коммит и пуш"                        │
└─────────────────────────────────────────────────┘
```

### 3.2. Workflow: Как они работают вместе

**Шаг 1: Планирование (Claude.ai)**
```
Ты: "Работаем над уведомлениями бота"
Claude.ai: Вот детальный план из 5 подзадач + файлы которые нужно создать/изменить
```

**Шаг 2: Исполнение (Claude Code на сервере)**
```
Ты копируешь задачу в Claude Code
Claude Code: Создаёт файлы, редактирует код, перезапускает сервисы, тестирует
```

**Шаг 3: Проверка (Claude.ai или ты вручную)**
```
Ты: "Проверь что получилось, вот лог ошибки"
Claude.ai: Анализирует, предлагает фикс → отправляешь в Claude Code
```

**Шаг 4: Документация (Claude.ai)**
```
Claude.ai: Обновляет CURRENT_STATE.md, напоминает коммит
```

### 3.3. CLAUDE.md — Ключевой файл для Claude Code

У тебя уже есть `CLAUDE.md` в корне проекта (коммит f7050e4). Это отлично — Claude Code читает его при старте. Нужно его обновить и расширить, чтобы Claude Code имел полный контекст. Вот что должно быть в нём:

```markdown
# CLAUDE.md — Project Context for Claude Code

## Project: GG HOOKAH v2
Telegram Mini App для доставки кальянов в Батуми.

## Architecture
- Backend API: Flask on port 5001 (gunicorn)
- Admin Panel: Flask on port 5002 (gunicorn)  
- Telegram Bot: aiogram 3.3.0 (polling)
- Frontend: React + TypeScript + Vite + Tailwind
- DB: PostgreSQL 16
- Server: Ubuntu 24.04, Nginx, systemd

## Key Paths
- Project: /opt/gg-hookah-v2/
- Venv: source /opt/gg-hookah-v2/venv/bin/activate
- Secrets: /etc/gg-hookah/.env
- Mini App build: /var/www/gghokah.delivery/
- Passport storage: /var/lib/gg-hookah/passports/

## Services
- systemctl restart gg-hookah-api
- systemctl restart gg-hookah-admin  
- systemctl restart gg-hookah-bot

## After Mini App changes
cd /opt/gg-hookah-v2/miniapp && npm run build
sudo rm -rf /var/www/gghokah.delivery/*
sudo cp -r dist/* /var/www/gghokah.delivery/
sudo chown -R www-data:www-data /var/www/gghokah.delivery

## DB Column Gotchas
- orders: NO total_amount column
- guests: column is "name" (NOT first_name)
- menu_items: column is "price_gel" (NOT price)
- rebowl_requests: column is "requested_at" (NOT created_at)

## Rules
- STRICTLY follow the spec (GG_HOOKAH_Spec_Master_v2_1.docx)
- Never invent features not in the spec
- Use placeholders for secrets: {{BOT_TOKEN}}, {{DB_PASSWORD}}
- Always activate venv before Python work
- Admin templates: currently standalone HTML (no extends)
- Git commit after each logical block of changes
```

### 3.4. Синхронизация Claude.ai ↔ Claude Code

**Проблема:** Claude.ai и Claude Code не видят друг друга напрямую.

**Решение — файл-мост:** `CURRENT_TASK.md` в корне проекта

```markdown
# CURRENT_TASK.md
## Текущая задача: M6 Этап 2 — Bot Notifications
## Статус: IN PROGRESS
## Подзадачи:
1. ✅ Создать notification service (bot/services/notifications.py)
2. 🔄 Подключить к admin routes/orders.py  
3. ⬜ Тестирование
## Заметки:
- [от Claude.ai] Используй HTTP запрос к боту, не импорт напрямую
- [от Claude Code] Файл создан, но бот не перезапущен
```

Claude Code видит этот файл при старте. Ты обновляешь его между сессиями.

---

## 4. ОБНОВЛЁННЫЙ ПЛАН РАБОТЫ (ПРИОРИТЕЗИРОВАННЫЙ)

Старый план (M7.7-M7.10 → M6 этапы 2-5) был линейным. Новый план учитывает **критический путь к рабочему MVP**.

### Фаза 1: ФУНДАМЕНТ (делает сервис юзабельным) — ~3-4 дня

| # | Задача | Почему критично | Сложность |
|---|--------|----------------|-----------|
| F1.1 | Admin template inheritance (base.html) | Исправляет B22, ускоряет всю дальнейшую работу | 🟡 Средняя |
| F1.2 | Notification service (admin → bot) | Без этого клиент ничего не знает | 🔴 Высокая |
| F1.3 | Background job: SESSION_ACTIVE → SESSION_ENDING | Без этого сессии не работают | 🟡 Средняя |
| F1.4 | users table integration (language sync) | Спека требует: RU/EN sync Mini App ↔ Bot | 🟡 Средняя |

### Фаза 2: КЛИЕНТСКИЙ ОПЫТ (бот становится полезным) — ~3-4 дня

| # | Задача | Описание | Сложность |
|---|--------|----------|-----------|
| F2.1 | Bot hot actions: Cancel, Ready for Pickup | Клиент может отменить/вернуть через бота | 🟡 Средняя |
| F2.2 | Bot hot actions: Free +1h, Rebowl request | Полный цикл сессии через бота | 🔴 Высокая |
| F2.3 | Support routing (messages → support_messages) | Приоритетная поддержка | 🟡 Средняя |
| F2.4 | Mini App — active order actions | Cancel/Pickup из Mini App | 🟢 Простая |

### Фаза 3: ADMIN ПАНЕЛЬ ПОЛНАЯ (операционная эффективность) — ~3-4 дня

| # | Задача | Описание | Сложность |
|---|--------|----------|-----------|
| F3.1 | Settings editor | Редактирование настроек без кода | 🟢 Простая |
| F3.2 | Guests management | Trust flag, passport, notes, history | 🟡 Средняя |
| F3.3 | Discounts & Promo Codes CRUD | Issue/withdraw discounts, create promo codes | 🟡 Средняя |
| F3.4 | Support inbox (Priority + General) | Чтение/ответ на сообщения в admin | 🟡 Средняя |
| F3.5 | Logs/System page | Audit log viewer, service status | 🟢 Простая |

### Фаза 4: ПОЛИРОВКА + PRODUCTION-READY — ~2-3 дня

| # | Задача | Описание | Сложность |
|---|--------|----------|-----------|
| F4.1 | Telegram initData validation | Безопасность API | 🟡 Средняя |
| F4.2 | Mini App UX fixes (skeleton loader, маскот размер) | Косметика | 🟢 Простая |
| F4.3 | Discount banner из API (вместо hardcoded) | Подключить к settings | 🟢 Простая |
| F4.4 | Dark theme toggle (спека Section 9) | Спека требует, но не критично для MVP | 🟡 Средняя |
| F4.5 | End-to-end тестирование по QA checklist | P6 из Prompts Pack | 🟡 Средняя |
| F4.6 | Error handling & edge cases | Production readiness | 🟡 Средняя |

---

## 5. КАК НАЧАТЬ РАБОТУ ПРЯМО СЕЙЧАС

### Шаг 1: Обнови CLAUDE.md на сервере через Claude Code

Открой Claude Code на сервере и попроси его:
```
Прочитай файл /opt/gg-hookah-v2/CLAUDE.md и обнови его содержимым [вставь обновлённый CLAUDE.md из секции 3.3]
```

### Шаг 2: Создай CURRENT_TASK.md

```
Создай файл /opt/gg-hookah-v2/CURRENT_TASK.md с информацией о текущей задаче F1.1 (admin template inheritance)
```

### Шаг 3: Начни с F1.1 — Admin Template Inheritance

Почему первым: это рефакторинг, который ускорит ВСЮ дальнейшую работу над admin панелью (F1.2, F3.1-F3.5). Вместо редактирования 11 файлов при каждом изменении sidebar — редактируешь 1 файл.

**Задание для Claude Code:**
```
Рефакторинг admin панели: создай base.html с общим layout (sidebar + topbar), 
затем переведи все шаблоны на {% extends "base.html" %}. 
Текущие шаблоны: login.html, dashboard.html, orders_list.html, order_detail.html, 
sessions_list.html, session_detail.html, menu_mixes.html, menu_mix_form.html, 
menu_drinks.html, menu_drink_form.html.
Не меняй визуал — только структуру шаблонов.
Заодно исправь B22: sidebar ссылки Menu должны вести на url_for('menu.mixes_list').
После изменений перезапусти gg-hookah-admin и проверь что все страницы работают.
```

### Шаг 4: Затем F1.2 — Notification Service

Это самая важная задача для MVP. Архитектурное решение:

```
Вариант A (рекомендую): HTTP API в боте
- Бот поднимает aiohttp сервер на localhost:5003
- Admin отправляет POST http://localhost:5003/notify с payload
- Бот формирует и отправляет сообщение клиенту

Вариант B: Shared database queue
- Admin пишет в таблицу notification_queue
- Бот полит таблицу каждые 5 секунд

Вариант A проще и быстрее. Не требует новых таблиц.
```

---

## 6. НУЖНЫ ЛИ ДОПОЛНИТЕЛЬНЫЕ ИИ-АГЕНТЫ?

### Для текущего этапа — НЕТ

Твой стек (Claude.ai Max + Claude Code) покрывает 100% задач:

- **Claude.ai** = архитектор, планировщик, ревьюер, документатор
- **Claude Code** = разработчик, DevOps, тестировщик

### Что может пригодиться в будущем:

| Инструмент | Когда | Зачем |
|-----------|-------|-------|
| Claude MCP (Model Context Protocol) | Когда подключишь Google Drive / Notion | Синхронизация документации |
| GitHub Actions | После MVP | CI/CD: автодеплой при push в main |
| Sentry | После запуска | Мониторинг ошибок в production |
| UptimeRobot | После запуска | Мониторинг доступности (бесплатный) |

---

## 7. ПЕРЕРАБОТАННЫЙ PROJECT_INSTRUCTIONS.txt

Твой текущий PROJECT_INSTRUCTIONS.txt хорош, но нуждается в обновлении под новый workflow. Вот ключевые изменения:

### Убрать:
- Инструкции про heredoc и VS Code (Claude Code решает эту проблему)
- "Не давать 10 команд разом" (Claude Code выполняет сам)

### Добавить:
- Роли Claude.ai vs Claude Code
- Ссылку на CURRENT_TASK.md
- Правило: Claude Code всегда читает CLAUDE.md перед работой
- Правило: после каждого блока работы Claude Code обновляет CURRENT_TASK.md

---

## 8. ИТОГО — TIMELINE

| Неделя | Фаза | Результат |
|--------|-------|----------|
| Неделя 1 | Фаза 1 (Фундамент) | Сервис работает end-to-end: заказ → уведомления → сессия → auto-ending |
| Неделя 2 | Фаза 2 (Клиентский опыт) | Клиент может управлять заказом через бота, поддержка работает |
| Неделя 3 | Фаза 3 (Admin полная) | Админ управляет всем: гости, скидки, промо, настройки |
| Неделя 4 | Фаза 4 (Полировка) | Production-ready MVP, тёмная тема, тесты |

**Реалистичная оценка: 3-4 недели до полного MVP** (при работе 3-4 часа/день с Claude Code).

---

## 9. ПЕРВОЕ ДЕЙСТВИЕ

Открой Claude Code на сервере и начни с:

```bash
# 1. Проверь что всё работает
systemctl status gg-hookah-api gg-hookah-admin gg-hookah-bot --no-pager
curl http://127.0.0.1:5001/health
curl http://127.0.0.1:5002/health

# 2. Обнови CLAUDE.md (Claude Code сделает это сам, если попросишь)
# 3. Начни задачу F1.1: Admin Template Inheritance
```

Удачи, Станислав! Ты на правильном пути. Проект имеет солидную базу — осталось достроить коммуникационный слой и операционные инструменты.
